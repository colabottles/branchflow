[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# GITHUB_TOKEN is set in the Netlify dashboard under
# Site configuration → Environment variables — never committed to the repo.